# Archived Utilities

These utilities are kept for reference only and are not part of the active
training pipeline:

- `create_val_split.py`:
  - superseded by `clipdetr/utils/split_yolo_dataset.py` (general train/val/test ratios)
- `print_split_stats.py`:
  - ad-hoc debug helper; replaced by `audit_detect_dataset.py` and run summaries

Use active utilities in `clipdetr/utils/` for current workflows.
